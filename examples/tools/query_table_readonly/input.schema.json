{
  "type": "object",
  "required": ["table", "select"],
  "additionalProperties": false,
  "properties": {
    "table": {
      "type": "string",
      "enum": ["customers", "tickets"]
    },
    "select": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "uniqueItems": true,
      "items": { "type": "string" }
    },
    "where": {
      "type": "object",
      "additionalProperties": {
        "type": ["string", "number", "boolean", "null"]
      }
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20
    }
  }
}
